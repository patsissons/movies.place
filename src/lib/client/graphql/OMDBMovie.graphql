query OMDBMovie($imdbId: String!) {
  omdbMovie(imdbId: $imdbId) {
    id
    response
    numericalRatings {
      id
      metascore
      imdbRating
      imdbVotes
      rottenTomatoesScore
    }
  }
}
