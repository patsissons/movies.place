query Configuration {
  configuration {
    images {
      baseUrl
      secureBaseUrl
      backdropSizes
      logoSizes
      posterSizes
      profileSizes
      stillSizes
    }
    changeKeys
  }
}
